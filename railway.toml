[build]
buildCommand = "pip install -r requirements.txt && playwright install chromium --with-deps"

[deploy]
startCommand = "PLAYWRIGHT_BROWSERS_PATH=/root/.cache/ms-playwright gunicorn flask_app:app"
